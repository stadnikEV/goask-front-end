<div class="{{className}}"
     data-component="{{componentName}}">
  <button class="button {{className}}__button
          {{#if modifierClassName}}
            {{modifierClassName}}
          {{/if}}"
          data-element="{{componentName}}__button">
   {{value}}
  </button>
</div>
