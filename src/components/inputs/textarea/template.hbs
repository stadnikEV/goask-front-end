<textarea class="textarea textarea-{{componentName}}"
          data-component="textarea-{{componentName}}"
          id="{{componentName}}"
          {{#if placeholder }}
            placeholder="{{placeholder}}"
          {{/if}}>
