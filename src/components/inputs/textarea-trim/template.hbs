<textarea class="textarea textarea-trim"
          data-component="textarea-trim-{{componentName}}"
          id="{{componentName}}"
          {{#if placeholder }}
            placeholder="{{placeholder}}"
          {{/if}}>
{{value}}
</textarea>
